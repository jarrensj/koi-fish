# Privy Configuration
# Get these from your Privy dashboard: https://dashboard.privy.io/
PRIVY_APP_ID=your_privy_app_id_here
PRIVY_APP_SECRET=your_privy_app_secret_here

# Solana Configuration
# You can use a free RPC endpoint like Helius, QuickNode, or Alchemy
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Wallet Configuration
# Generate a new wallet keypair and add the secret key here
# You can generate one using: solana-keygen new --outfile ~/my-wallet.json
# Then convert to base58: solana-keygen pubkey ~/my-wallet.json --keypair ~/my-wallet.json | base58
WALLET_SECRET_KEY_BASE58=your_wallet_secret_key_base58_here

# Alternative: You can also use JSON format
# WALLET_SECRET_KEY_JSON=[1,2,3,4,5,...] # 64 numbers representing the secret key
