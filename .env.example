# ============================================================
#  Core Environment
# ============================================================
NODE_ENV=development
# Set to "production" when deploying live (affects logging, etc.)

# ============================================================
#  Solana RPC Endpoint
# ------------------------------------------------------------
#  mainnet: https://api.mainnet-beta.solana.com
#  devnet : https://api.devnet.solana.com
#  local  : http://localhost:8899
# ============================================================
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# ============================================================
#  Wallet Configuration
# ------------------------------------------------------------
#  Choose ONE option below to unlock your Solana wallet:
#   - WALLET_SECRET_KEY_BASE58 : Phantom/Brave private key export
#   - WALLET_SECRET_KEY_JSON   : 64-number JSON array from `solana-keygen`
#  NEVER commit real keys or push them to Git.
# ============================================================
WALLET_SECRET_KEY_BASE58=
WALLET_SECRET_KEY_JSON=

# ============================================================
#  Safety / Swap Controls
# ------------------------------------------------------------
#  DRY_RUN=true  → sign transactions but DO NOT send to chain
#   - true = build+sign but DO NOT send; false = send it
#  SLIPPAGE_BPS  → swap tolerance; 100 = 1% slippage
#  PRIORITY_FEE_MICROLAMPORTS → optional Solana tip for faster confirms
# ============================================================
DRY_RUN=true
SLIPPAGE_BPS=100
PRIORITY_FEE_MICROLAMPORTS=0

# ============================================================
#  Honeypot / Safety Gate
# ------------------------------------------------------------
#  SAFETY_CHECK=true → run the honeypot pre-trade checks
#  HONEYPOT_MODE     → behavior mode for honeypot checker:
#      off    = completely skip honeypot checks (fastest for dev)
#      warn   = run checks but never block; converts errors to warnings
#      strict = original blocking behavior (recommended for prod)
#  FORCE_UNSAFE=true → override all safety blocks (use with caution)
# ============================================================
SAFETY_CHECK=true
HONEYPOT_MODE=warn
FORCE_UNSAFE=false

# ============================================================
#  Jupiter API (Swapping Engine)
# ------------------------------------------------------------
#  You can override Jupiter endpoints for testing if needed.
#  If JUP_QUOTE_URL/JUP_SWAP_URL are set, JUP_API_BASE is ignored.
# ============================================================
JUP_API_BASE_URL=
JUP_QUOTE_URL=https://lite-api.jup.ag/swap/v1/quote
JUP_SWAP_URL=https://lite-api.jup.ag/swap/v1/swap

# Optional input mint (default = Wrapped SOL)
INPUT_MINT=So11111111111111111111111111111111111111112

# ============================================================
#  DexScreener API
# ------------------------------------------------------------
#  Used for liquidity / market-cap checks and price tracking.
# ============================================================
DEXSCREENER_BASE_URL=https://api.dexscreener.com

# ============================================================
#  Price Tracker / Auto Trader
# ------------------------------------------------------------
#  price:track uses:
#    POLL_INTERVAL_MIN + TARGET_UP_PCT
#  trade:auto uses:
#    LOG_INTERVAL_MIN + TARGET_UP_PCT + TRIM_PCT
# ============================================================
POLL_INTERVAL_MIN=5
LOG_INTERVAL_MIN=15
TARGET_UP_PCT=20
TRIM_PCT=25



# ============================================================
#  Registry (Optional)
# ------------------------------------------------------------
#  Local allow/deny list for canonical tokens.
#  Default file path: config/registry.json
#  You can override by setting REGISTRY_PATH below.
# ============================================================
REGISTRY_PATH=config/registry.json


# ============================================================
#  Server
# ------------------------------------------------------------
# ============================================================
PORT=3000
API_KEY=change-me-please       # frontend must send x-api-key: change-me-please
CORS_ORIGIN=*                  # set to your FE origin in production