# ============================================================
#  Core Environment
# ============================================================
# Set to "production" in live deployments (affects logging, etc.)
NODE_ENV=development

# ============================================================
#  Solana RPC Endpoint  (REQUIRED)
# ------------------------------------------------------------
#  mainnet: https://api.mainnet-beta.solana.com
#  devnet : https://api.devnet.solana.com
#  local  : http://localhost:8899
# ============================================================
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# ============================================================
#  Wallet Configuration (REQUIRED: choose exactly ONE)
# ------------------------------------------------------------
#  Option A: WALLET_SECRET_KEY_BASE58
#    - Phantom/Brave exported secret (base58). Must decode to 64 bytes.
#  Option B: WALLET_SECRET_KEY_JSON
#    - 64-number JSON array from `solana-keygen` (e.g., [12,34,...]).
#  IMPORTANT: Never commit your real secrets.
# ============================================================
WALLET_SECRET_KEY_BASE58=
# WALLET_SECRET_KEY_JSON=[12,34, ... 64 numbers total ...]

# ============================================================
#  Swap Controls (OPTIONAL)
# ------------------------------------------------------------
#  DRY_RUN=true  → build+sign but DO NOT broadcast (safe default); false = send it
#  SLIPPAGE_BPS  → swap tolerance (100 = 1% slippage)
#  PRIORITY_FEE_MICROLAMPORTS → optional tip (micro lamports) for faster confirms
# ============================================================
DRY_RUN=true
SLIPPAGE_BPS=100
PRIORITY_FEE_MICROLAMPORTS=0

# ============================================================
#  Jupiter API (OPTIONAL)
# ------------------------------------------------------------
#  These default to Jupiter's lite API. Override only if testing.
#  If JUP_QUOTE_URL / JUP_SWAP_URL are set, base is ignored.
# ============================================================
# JUP_API_BASE_URL=https://lite-api.jup.ag
JUP_QUOTE_URL=https://lite-api.jup.ag/swap/v1/quote
JUP_SWAP_URL=https://lite-api.jup.ag/swap/v1/swap

# ============================================================
#  HTTP Server (RECOMMENDED)
# ------------------------------------------------------------
#  API_KEY       → if set, requests must send header:  X-API-Key: <value>
#  CORS_ALLOWED_ORIGINS → comma-separated list; "*" allows all (dev only)
# ============================================================
PORT=3001
API_KEY=change-me-please
CORS_ALLOWED_ORIGINS=*

# ============================================================
#  Notes
# ------------------------------------------------------------
# - Required vars for this PR: SOLANA_RPC_URL and one wallet secret
#   (WALLET_SECRET_KEY_BASE58 or WALLET_SECRET_KEY_JSON).
# - DRY_RUN=true is safest for testing; set false to actually send the swap.
# - Keep this file named `.env.local` and add it to .gitignore.
# ============================================================
